#include <iostream>
using namespace std;
int main (){

     int n;
     cout<<"enter the distance in km";
     cin>>n;
     int meter=(n*1000);
     cout<<"the distance in meter is "<<meter;
     int centimeter = (meter*100);
     cout<<"the distance in centimeter "<<centimeter;

    return 0;
}